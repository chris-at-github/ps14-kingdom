plugin.tx_kingdom_frontend {
    view {
        # cat=plugin.tx_kingdom_frontend/file; type=string; label=Path to template root (FE)
        templateRootPath = EXT:kingdom/Resources/Private/Templates/
        # cat=plugin.tx_kingdom_frontend/file; type=string; label=Path to template partials (FE)
        partialRootPath = EXT:kingdom/Resources/Private/Partials/
        # cat=plugin.tx_kingdom_frontend/file; type=string; label=Path to template layouts (FE)
        layoutRootPath = EXT:kingdom/Resources/Private/Layouts/
    }
    persistence {
        # cat=plugin.tx_kingdom_frontend//a; type=string; label=Default storage PID
        storagePid =
    }
}
